переименовать EngineContext.h, .cpp -> MainContext.h, .cpp

пофиксить падение фпс при остановке симуляции с Fps limit

порефакторить input (включая редактор), починить понг

поиск ноды под курсором: traverse scene with Visual::HitTest, return ordered list (top-to-bottom)

перемещение камеры по ПКМ

персистентность редактора

загрузка/сохранение сцены

оптимизировать debug-draw

todos в коде

поревьюить инициализацию нод и сущностей

PhysicsProcessor::Run(const sf::Time& dt) удаление протухших при итерации