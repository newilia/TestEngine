куча todos в коде

пересмотреть неймспейсы Editor, Engine, App, Common (common.h), Utils

добавить LOG_DEBUG(), LOG_INFO() и т.д. и панельку с логом

невыпуклые многоугольники в Polygon tool

кривая работа столкновений тел, если их глобальный трансформ не равен identity

кастомные инспекторы для типов

isEnabledLocal, isVisibleLocal

эффект свечения

эффект радиационного излучения

propery animator

-интеграция box2d

отображать в нод инспекторе полупрозрачным заголовком список сущностей, которые есть не на всех нодах, для их редактирования

добавить режим следования камеры за центром масс нескольких объектов

move tool multiselect, синхронное перемещение нескольких нод

